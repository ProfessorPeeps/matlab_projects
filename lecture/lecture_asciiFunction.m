%% Lecture - 2/14/18
%% Functions

format compact, format short

%harcoded array
%charArray = ['10','$','c','&'];
%ascii(charArray)

%prompt user to enter inputs

charInput = input('Enter a character (no spaces): ','s'); 
ASCII = ascii(charInput);
display('ASCII code equivalent: ');
display(ASCII)
